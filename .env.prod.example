# Domínio do seu VPS (sem https://)
DOMAIN=seudominio.com

# JWT — gere com: openssl rand -hex 64
JWT_SECRET=troque-por-uma-chave-muito-longa-e-aleatoria
JWT_EXPIRES_IN=1d

# Banco de dados
DB_USER=postgres
DB_PASSWORD=troque-por-uma-senha-forte
DB_NAME=patient_db

# URL da API vista pelo navegador (com https)
VITE_API_URL=https://seudominio.com/api
